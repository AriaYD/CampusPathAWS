{
  "$id": "https://campuspath.invalid/schema/SurfaceConversion.json",
  "additionalProperties": false,
  "description": "某一个入口（广场 / 为你推荐）上的「看见 → 行动」转化（Spec §17.6）。\n\n嵌在 :class:`MetricTuple` 里而不是另开一条流：广场转化的分母是**全部**\n广场曝光，不满足 ``seen ≤ eligible`` 那条嵌套约束（学生在广场上看到的\n包括他并不合格的机会——而那恰恰是校方最想知道的信号）。放进带自己\n局部不变量的子模型，既保住「每人每期一条元组」，也不动现有 validator。",
  "properties": {
    "acted_count": {
      "minimum": 0,
      "title": "Acted Count",
      "type": "integer"
    },
    "exposed_count": {
      "minimum": 0,
      "title": "Exposed Count",
      "type": "integer"
    },
    "surface": {
      "enum": [
        "plaza",
        "for_you"
      ],
      "title": "Surface",
      "type": "string"
    }
  },
  "required": [
    "surface",
    "exposed_count",
    "acted_count"
  ],
  "title": "SurfaceConversion",
  "type": "object"
}
